/*
 * BookDetailsPage Messages
 *
 * This contains all the text for the BookDetailsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BookDetailsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BookDetailsPage container!',
  },
});
