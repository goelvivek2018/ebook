/*
 * GoogleAuth Messages
 *
 * This contains all the text for the GoogleAuth container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GoogleAuth';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GoogleAuth container!',
  },
});
